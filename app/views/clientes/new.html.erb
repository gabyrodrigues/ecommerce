<%if !notice.blank?%>
    <p id="notice"><%= notice %></p>
<% end %>

<div class="form-container -cliente">
    <div class="wrapper">
        <div class="heading">
            <h1 class="legend">Cadastro de cliente</h1>
            <p class="note">Cadastre um cliente na loja e ele recebe um email de confirmação de cadastro</p>
        </div>

        <%= form_with(url: "/clientes/criar", method: "get", class: "form-register") do |f| %>
            <div class="block -admin">
                <fieldset class="fieldset -name">
                    <%= f.label :nome, "Nome", class: "label" %>
                    <%= f.text_field :nome, class: "input", name: "nome", id: "nome", :required => true %>
                </fieldset>

                <fieldset class="fieldset -email">
                    <%= f.label :email, "Email", class: "label" %>
                    <%= f.text_field :email, class: "input", name: "email", id: "email", :required => true %>
                </fieldset>
            </div>

            <div class="actions">
                <%= f.submit "Salvar", class: "action -save" %>
            </div>
        <% end %>

        <div class="actions">
            <%= link_to '<i class="fas fa-reply"></i> Voltar'.html_safe, clientes_path, class: "action -back" %>
        </div>
    </div>
</div>
