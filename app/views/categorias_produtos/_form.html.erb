<%= form_with(model: categorias_produto, local: true) do |form| %>
  <% if categorias_produto.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(categorias_produto.errors.count, "error") %> prohibited this categorias_produto from being saved:</h2>

      <ul>
        <% categorias_produto.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :categoria_id %>
    <%= form.text_field :categoria_id %>
  </div>

  <div class="field">
    <%= form.label :produto_id %>
    <%= form.text_field :produto_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
