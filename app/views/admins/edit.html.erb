<div class="form-container -admin">
    <div class="wrapper">
        <div class="heading">
            <h1 class="legend">Editando administrador</h1>
        </div>

        <%= render 'form', admin: @admin %>

        <div class="actions">
            <%= link_to 'Ver admin', admim_path(@admin), class: "action -show" %>
            <%= link_to '<i class="fas fa-reply"></i> Voltar'.html_safe, admins_path, class: "action -back" %>
        </div>
    </div>
</div>
